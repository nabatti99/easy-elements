const { createProxyMiddleware } = require("http-proxy-middleware");

module.exports = app => {
  // app.use(
  //   "/api",
  //   createProxyMiddleware({
  //     target: "https://artlist.io",
  //     secure: true,
  //     changeOrigin: true
  //   })
  // )
}